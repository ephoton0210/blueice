window-title-default = BlueIce (reference frontend)
window-title-navigated = BlueIce -- { $url }
# Deliberately not translated in zh-TW/frontend.ftl -- a real fixture
# for a partially-translated locale, exercising translate()'s per-key
# fallback (see i18n/src/lib.rs's own tests). Not used by product code.
only-in-english = This key intentionally has no zh-TW translation yet.
